{
  "name": "devweave",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently -k -p \"[{name}]\" -n \"BACKEND,FRONTEND\" -c \"cyan.bold,magenta.bold\" \"npm --prefix devweave-backend run dev\" \"npm --prefix devweave-app run dev\"",
    "dev:backend": "npm --prefix devweave-backend run dev",
    "dev:frontend": "npm --prefix devweave-app run dev",
    "build": "npm --prefix devweave-backend run build && npm --prefix devweave-app run build",
    "test": "npm --prefix devweave-backend run test"
  },
  "dependencies": {
    "@prisma/client": "^7.10.0",
    "prisma": "^7.10.0"
  }
}
